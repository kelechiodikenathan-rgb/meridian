// Cloudflare Pages / local configuration.
// For Direct Upload, replace these placeholders with your Supabase project URL and PUBLIC anon/publishable key.
// For Git-connected Cloudflare Pages deployments, the build script overwrites this file from environment variables.
window.MERIDIAN_CONFIG = {
  supabaseUrl: 'YOUR_SUPABASE_URL',
  supabaseAnonKey: 'YOUR_SUPABASE_ANON_KEY'
};
